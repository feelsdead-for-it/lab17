#include <iostream>
#include <cmath>
using namespace std;
int main()
{
	int n,m,z,sum,b,i;
	cin >> n;
	m = 0;
	z = 0;
	sum = 0;
	int * x = new int[n];
	int * y = new int[n];
	cout << "(x y): \n";
	for (i = 0; i < n; i++)
	{
		cout << i + 1 << "  ";
		cin >> x[i] >> y[i];
		if ((x[i] < 0) && (y[i] > 0) && (sqrt(pow(x[i], 2) + pow(y[i], 2)) > sum))
		{
			sum = sqrt(pow(x[i], 2) + pow(y[i], 2));
			b = i;
		}
	}
	if (sum > 0)
	{
		cout << "  " << x[b] << ", " << y[b] << "2 ch " << sum << " from start" << endl;
	}
	else
	{
		cout << "(0, 0)" << endl;
	}
}
